# Example environment file for Mystic Tarot
# Copy to .env.local and fill in your values

# Database URL (only needed if you use Prisma features)
DATABASE_URL="file:./dev.db"

# NextAuth (only needed if you add authentication)
# NEXTAUTH_SECRET="your-secret-here"
# NEXTAUTH_URL="http://localhost:3000"
